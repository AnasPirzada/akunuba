'use client';
// Re-export MarketplacePage to handle active-offers route
// This allows the same component to handle both /marketplace and /marketplace/active-offers routes
export { default } from '../page';
